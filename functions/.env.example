# Google Places API Key
# Get your key from: https://console.cloud.google.com/apis/credentials
# Enable: Places API (New) and Geocoding API
GOOGLE_PLACES_API_KEY=your_api_key_here

# Mapbox Secret Token
# Get your token from: https://account.mapbox.com/access-tokens/
# Required for: Directions API, Map Matching API, and Elevation data
MAPBOX_SECRET_TOKEN=your_mapbox_token_here

# IMPORTANT: 
# 1. Copy this file to .env for local development
# 2. Add .env to .gitignore (never commit API keys!)
# 3. For production deployment, set secrets in Firebase panel (left sidebar) > Functions > Secrets
# 4. Restrict API keys to Cloud Functions domain in respective consoles
